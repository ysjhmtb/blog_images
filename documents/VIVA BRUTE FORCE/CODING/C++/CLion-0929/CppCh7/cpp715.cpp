#include <iostream>
using namespace std;


void cpp7151(){

}


/*

int main(int argc, char *argv[]) {

    // ... C++/CLion-0929/cmake-build-debug/CLion_0929 실행파일 이름이 나온다.
    for (int count = 0; count < argc; ++count) {
        cout << argv[count] << endl;
    }

    // 0x7ffeeed01a40
    for (int count = 0; count < argc; ++count) {
        std::string argv_single = argv[count];
        cout << argv << endl;
    }

    // 0x7ffee98c8a40
    for (int count = 0; count < argc; ++count) {
        std::string argv_single = argv[count];

        if (count == 1) {
            int input_number = std::stoi(argv_single);
            cout << input_number + 1 << endl;
        }else
        {
            cout << argv << endl;
        }
    }


    cpp7151();

    return 0;
}
 */