# 19.2 C++ 17 함수에서 여러 개의 리턴값 반환하기





```C++

// main.cpp

#include <iostream>
#include <tuple>

//std::tuple<int, int> my_func() {
//    return std::tuple<int, int>(123, 456);
//}

//auto my_func() {
//    return std::tuple<int, int>(123, 456);
//}

// C++ 17
auto my_func() {
    return std::tuple(123, 456, 789, 10);
}

int main() {

    // std::tuple<int, int> result = my_func();

    // auto result = my_func();
    // std::cout << std::get<0>(result) << " " << std::get<1>(result) << " " << std::get<2>(result) << std::endl;

    auto[a, b, c, d] = my_func();
    std::cout << a << " " << b << " " << c << " " << d << " " << std::endl;

    return 0;
}



```

