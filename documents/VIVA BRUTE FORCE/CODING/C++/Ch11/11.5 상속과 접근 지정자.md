# 11.5 상속과 접근 지정자





```C++

// main.cpp

#include <iostream>

using namespace std;


class Base {
public:
    int m_public;

protected:
    int m_protected;

private:
    int m_private;
};

class Derived : private Base {

public:
    Derived() {
        Base::m_public;
        Base::m_protected;
    }
};


class GrandChild : public Derived{
public:
    GrandChild(){
//        Derived::m_public; // error. class Derived : private Base.
                             // private Base 때문에 Derived() 안의 Base::m_public;이 private으로 인식됨.
    }
};


int main() {

    Derived derived;
    // derived.m_public = 1024; // error because of class Derived : protected Base




    return 0;
}

```

