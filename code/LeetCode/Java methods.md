- Map - put / containsKey / containsValue / entrySet / isEmpty / keySet / values / remove / get 
- throw new IllegalArgumentException("No two sum solution");
- Arrays.fill(one, Integer.MAX_VALUE);
- 

